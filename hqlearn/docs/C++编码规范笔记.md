## 线程局部变量
在C++中```thread_local```是一个存储类说明符，用于指定变量具有线程本地存储器。每个线程都将拥有该变量的独立示例。

注意事项：
-   ```static thread_local```：变量具有内部链接，仅在当前编译单元可见
-   ```extern thread_local```：变量具有外部链接，可以跨编译单元使用
-   在类成员中使用时，必须声明为 static
-   线程局部变量的初始化和销毁成本可能较高，不宜过度使用。
-   C++11 及以上标准支持 thread_local 关键字，旧标准（如 C++03）不支持。